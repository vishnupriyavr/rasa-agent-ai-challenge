flows:
  get_transit_route:
    description: Help the user to navigate between locations.
    steps:
      - collect: spoken_language
      - collect: current_location
      - collect: destination_location
      - collect: transit_mode
      - action: "action_provide_transit_details"